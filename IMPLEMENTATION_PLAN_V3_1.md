# coinchart V3.1 분석 프레임워크 이식 계획

## 목표

업그레이드한 `AI MASTER CRYPTO TRADING ANALYST V3.1`의 핵심 원칙을 현재 `coinchart`의 실제 계산과 화면에 이식한다. 기존 차트·VPVR·엘리엇 기능과 종목별 상태 격리는 유지하고, 확인 가능한 데이터만 조건부 매매 브리핑으로 통합한다.

## 구현 범위

1. **재현 가능한 V3.1 구조 분석 모듈**
   - 확정봉만 입력받는 무상태 모듈을 추가한다.
   - Wilder ADX(14)와 +DI/-DI, MA20/50/200, SuperTrend를 계산한다.
   - 좌우 5개 확정봉 피벗, BOS, FVG 메움 상태, 오더블록 후보/무효화를 계산한다.
   - 최근 확정 스윙으로 피보나치 0.382/0.5/0.618/0.786을 계산한다.
   - 선택 종목의 데이터만 사용하며 이전 종목 결과를 재사용하지 않는다.

2. **V3.1 5부 브리핑 패널**
   - 분석 기준, 추세·유동성, 모멘텀·파동, 파생상품, 펀더멘털, 조건부 롱/숏·관망을 한 패널에 표시한다.
   - 가격은 USDT를 먼저 표시하고, 검증된 USDT/KRW 환율이 있을 때만 KRW를 괄호 안에 표시한다.
   - 거래소·시장·기준 시각·마지막 확정봉·환율 출처를 함께 표시한다.
   - 청산맵·CVD·온체인·언락처럼 사이트에서 검증하지 못한 항목은 `현재 실시간 데이터 확인 불가`로 표시한다.
   - 근거 없는 승률·확률·레버리지는 만들지 않는다.

3. **조건부 실행 시나리오**
   - 확인된 지지·저항/FVG/오더블록을 기준으로 롱과 숏 또는 관망의 발동 조건·무효화·목표를 구간으로 제시한다.
   - 충분한 구조 근거가 없으면 단일 진입 숫자를 만들지 않고 관망으로 처리한다.
   - 펀딩비와 OI는 현재 선택한 거래소·계약의 실제 응답과 시각이 있을 때만 해석한다.

4. **기존 기능 보호와 회귀 검증**
   - BTC에서 ETH/SOL 등으로 변경할 때 엘리엇과 V3.1 결과의 `data-symbol`·시간봉이 함께 바뀌는지 검증한다.
   - 기존 분석 엔진·백테스트·엘리엇 회귀 테스트를 모두 실행한다.
   - 데스크톱과 모바일에서 레이아웃 넘침, 콘솔 오류, 데이터 없음 처리를 브라우저로 확인한다.

5. **백업·배포**
   - 변경 전 전체 Git 이력을 번들 파일로 백업한다.
   - 검증 완료 후 변경 파일만 커밋하여 `main`에 푸시한다.
   - GitHub Pages 배포 완료와 공개 URL의 V3.1 표시를 다시 확인한다.

## 완료 기준

- 모든 로컬 테스트 통과
- BTC→ETH→SOL 전환 시 V3.1/엘리엇 종목 문맥 일치
- 완료봉·출처·시각·데이터 없음 정책 표시
- 모바일 가로 넘침 없음
- GitHub Pages 공개본에서 V3.1 패널 확인

## 로컬 검증 결과

- 분석 엔진·백테스트·엘리엇·V3.1 단위/통합 테스트 5종 통과
- BTC→ETH→SOL 전환 시 V3.1과 엘리엇의 종목 데이터 속성 일치
- 390px 모바일 화면 가로 넘침 0px
- 새 브라우저 세션 콘솔 오류·경고 0건
- MEXC 전체 티커·경제 캘린더·매크로 프록시처럼 CORS가 차단된 반복 요청은 실행하지 않고 `현재 확인 불가`로 표시

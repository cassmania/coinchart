# 코인차트 분석 정확도 개선 구현 계획

작성일: 2026-08-17
대상 사이트: https://cassmania.github.io/coinchart/  
대상 저장소: https://github.com/cassmania/coinchart

## 1. 현재 구조 진단

현재 사이트는 `index.html` 한 파일에 화면, 거래소 데이터 수집, 보조지표 계산, 지지·저항, 파동 분석, 종합 신호가 함께 들어 있습니다. 화면 기능은 풍부하지만 아래 항목 때문에 표시값과 매매 신호의 재현성 및 검증 가능성이 낮습니다.

### 우선순위 매우 높음

1. **진행 중인 캔들로 신호가 계속 바뀜**
   - 실시간 봉의 종가·고가·저가가 변할 때 RSI, MACD, SuperTrend, 파동, 종합 신호가 다시 계산됩니다.
   - 화면용 실시간 값과 매매 판단용 확정봉 값을 분리하지 않아 신호가 봉 마감 전에 발생했다가 사라질 수 있습니다.

2. **RSI가 일반적으로 사용하는 Wilder 방식이 아님**
   - 현재 RSI는 최근 14개 등락을 매번 단순 평균으로 계산합니다.
   - TradingView 등에서 일반적으로 표시되는 Wilder RSI와 값이 달라질 수 있습니다.

3. **EMA 초기값과 MACD 판정이 단순함**
   - EMA를 첫 번째 종가로 시작하여 초기 구간 편향이 생깁니다.
   - 종합 신호는 MACD선이 0보다 큰지만 보고, MACD선과 시그널선의 교차 및 히스토그램 방향을 반영하지 않습니다.

4. **VWAP가 세션 구분 없이 최대 500개 봉 전체를 누적함**
   - 1분봉과 일봉에서 같은 방식으로 누적되므로 지표 의미가 시간봉마다 달라집니다.
   - 일중 VWAP는 UTC 날짜별로 초기화하고, 주봉·월봉 등에서는 앵커 기준을 명시해야 합니다.

5. **VPVR 거래량 배분이 지나치게 단순함**
   - 각 캔들의 전체 거래량을 `(고가+저가+종가)/3` 한 가격 구간에 몰아서 넣습니다.
   - 실제 캔들이 통과한 가격 범위에 거래량을 분산하지 않아 POC, VAH, VAL, HVN이 크게 달라질 수 있습니다.
   - 현재 화면에 보이는 범위가 아니라 전체 로드 구간을 사용하므로 엄밀한 의미의 Visible Range Volume Profile이 아닙니다.

6. **피벗 지지·저항 계산 기준이 표준 피벗과 다름**
   - 현재 봉의 HLC와 최근 20봉 최고·최저를 섞어 R1/R2/R3와 S1/S2/S3를 계산합니다.
   - 표준 피벗처럼 이전에 완전히 종료된 기준 기간의 HLC를 사용하지 않아 선이 현재가 주변에서 자주 왜곡됩니다.

7. **백테스트 결과의 적용 범위가 맞지 않음**
   - 코드 주석에는 15개 심볼·일봉 결과라고 적혀 있지만, 사이트는 같은 기대값 표를 1분봉부터 월봉까지 적용합니다.
   - 백테스트 엔진과 현재 파동 탐지 엔진의 구현이 다르다는 점도 코드에 명시되어 있어 기대값을 그대로 재사용하기 어렵습니다.
   - 재현 가능한 데이터셋, 실행 스크립트, 표본 수, 수수료·슬리피지 적용 결과가 저장소에 없습니다.

8. **종합 신호가 서로 강하게 연관된 추세 지표를 중복 가산함**
   - MA 정렬, MA200 위치, MACD 0선, SuperTrend가 같은 추세 정보를 여러 번 더합니다.
   - 점수 임계값과 가중치가 백테스트로 교정되지 않아 `강한 매수/매도` 문구가 실제 확률을 뜻하지 않습니다.

9. **거래소별 OI 단위가 통일되지 않음**
   - 거래소마다 계약 수, 코인 수량 등 단위가 다른 값을 동일한 숫자 형식으로 표시합니다.
   - 절대 OI만 표시하고 변화율이 없어 방향성 판단 근거로 쓰기 어렵습니다.

## 2. 단계별 구현 계획

한 번에 전체 구조를 바꾸지 않고, 각 단계의 테스트가 통과한 뒤 다음 단계로 진행합니다.

### 1단계 — 확정봉 기반 표준 보조지표 엔진 ✅ 완료

완료일: 2026-08-17

구현 결과:

- `analysis-engine.js`에 표준 계산 엔진을 분리했습니다.
- RSI와 ATR은 Wilder RMA, EMA는 SMA 시드, MACD는 시그널선·히스토그램을 포함하도록 통일했습니다.
- 시간봉별 앵커 VWAP 기준을 화면에 표시합니다.
- 진행 중인 봉은 차트와 현재가에는 표시하지만 보조지표·종합 신호·파동 판단에서는 제외합니다.
- 분할 차트가 마감된 캔들을 실시간 가격으로 계속 덮어쓰지 않도록 새 캔들을 다시 조회합니다.
- 백테스트 기대값은 검증된 일봉에서만 표시하고 다른 시간봉은 `미검증`으로 관망 처리합니다.
- `tests/analysis-engine.test.js`로 RSI, EMA, MACD, ATR, SuperTrend, VWAP, 확정봉 불변성을 검증했습니다.
- Chrome 실제 구동 점검에서 확정봉 기준 시각과 VWAP 앵커 문구가 정상 표시되고 JavaScript 오류가 없음을 확인했습니다.

수정 범위:

- 화면 표시용 `liveCandles`와 신호 계산용 `closedCandles`를 분리합니다.
- RSI를 Wilder RMA 방식으로 교체합니다.
- EMA 초기값을 SMA 시드 방식으로 교체합니다.
- MACD선, 시그널선, 히스토그램을 같은 엔진에서 계산합니다.
- ATR을 Wilder 방식으로 통일합니다.
- VWAP를 시간봉별 명시적 앵커 기준으로 계산합니다.
- 계산 함수는 화면 코드와 분리하여 독립 테스트가 가능하게 만듭니다.
- 기존 화면 배치와 차트 사용법은 유지합니다.

검증 기준:

- 고정 OHLCV 샘플에 대해 RSI, EMA, MACD, ATR, VWAP 예상값 테스트를 추가합니다.
- 입력 캔들을 한 개씩 추가했을 때 과거 확정값이 바뀌지 않아야 합니다.
- 진행 중인 봉이 바뀌어도 `확정봉 신호`는 봉 마감 전까지 바뀌지 않아야 합니다.
- 화면에는 `실시간 참고값`과 `확정봉 신호`의 기준 시각을 구분해 표시합니다.

### 2단계 — VPVR 및 지지·저항 개선 ✅ 완료

완료일: 2026-08-17

구현 결과:

- 캔들의 전체 거래량을 대표가격 한 칸에 넣던 방식을 제거하고, 고가~저가와 겹치는 가격 구간에 비례 배분합니다.
- POC 중심 70% Value Area를 계산하고 VAH·VAL 경계를 가격 구간의 실제 상단·하단으로 반환합니다.
- HVN은 평균 거래량 기준을 넘는 지역 고점만 선택하고, LVN도 계산 엔진에서 별도로 산출합니다.
- 메인 VPVR은 `표시구간`과 `최근 180봉` 모드를 전환할 수 있습니다.
- 표시구간이 60봉 미만이면 통계 불안정을 피하기 위해 최근 180개 확정봉으로 자동 전환하고 화면에 표시합니다.
- VPVR 막대는 전체 가격범위 비례좌표가 아니라 차트의 실제 가격 좌표에 맞춰 그립니다.
- 피벗은 현재 봉이 아니라 이전에 완전히 종료된 UTC 일봉·주봉·월봉·연봉의 HLC로 계산합니다.
- `연간 고점/저점`처럼 조회 범위와 맞지 않던 명칭을 `최근 30봉`, `최근 100봉`, `조회구간`으로 수정했습니다.
- 근접 레벨 병합 허용폭은 ATR 기반으로 조정하고, 강도는 점수 대신 실제로 겹친 근거 수로 표시합니다.
- 분할 차트도 동일한 거래량 배분 POC·VAH/VAL·표준 피벗을 사용하도록 통일했습니다.
- 단위 테스트로 거래량 총합 보존, 가격구간 배분, 정확한 70% Value Area, 이전 완료 기간 피벗을 검증했습니다.
- Chrome 실제 구동 점검에서 VPVR 모드, POC, 지지·저항 목록이 정상 표시되고 JavaScript 오류가 없음을 확인했습니다.

수정 범위:

- 캔들 거래량을 고가~저가와 겹치는 가격 구간에 비례 배분합니다.
- Visible Range와 Fixed Range를 구분하고 현재 사용 모드를 화면에 표시합니다.
- POC 중심 70% Value Area 계산을 검증 가능한 함수로 분리합니다.
- 피벗은 이전에 종료된 기준 기간 HLC로 계산합니다.
- `연간 고점`처럼 실제 조회 범위와 다른 명칭을 `최근 100봉 고점`처럼 정확하게 바꿉니다.
- 지지·저항의 강도는 단순 점수 대신 겹친 근거 수와 허용 오차를 함께 표시합니다.

검증 기준:

- 인위적으로 거래량이 몰린 샘플에서 POC가 해당 가격 구간에 형성되어야 합니다.
- VAH/VAL 내부 거래량이 전체의 약 70%가 되어야 합니다.
- 지지선은 현재가 아래, 저항선은 현재가 위에만 배치되어야 합니다.
- 시간봉 변경 시 조회 범위와 레벨 명칭이 일치해야 합니다.

### 3단계 — 종합 신호와 파동 기대값 재검증 ✅ 완료

완료일: 2026-08-17

구현 결과:

- MA 배열·MA200·MACD·SuperTrend를 각각 더하던 중복 점수 방식을 제거했습니다.
- 추세·모멘텀·변동성·거래량을 각각 -1~+1 한 축으로 정규화한 뒤 동일 비중으로 합칩니다.
- `강한 매수/강한 매도` 문구를 없애고 `상승/하락 추세 우위` 또는 `관망`으로 표시합니다.
- 펀딩비는 합성 점수에서 제외하고 과열 참고 문맥으로만 표시합니다.
- 현재 사이트의 파동 탐지기와 기존 파동 백테스트 엔진이 다르므로, 기존 기대값과 매매안은 재현 검증 전까지 비활성화했습니다.
- 화면과 백테스트가 같은 `analysis-engine.js`의 `compositeSignal()`을 사용합니다.
- Bybit USDT 무기한 선물 10종의 일봉을 고정 스냅샷으로 저장하고 파일별 SHA-256 해시를 기록했습니다.
- 신호 다음 봉 시가 체결, 편도 수수료 0.060%, 편도 슬리피지 0.020%, 2 ATR 손절, 1.5R 목표, 최대 20봉 보유 규칙을 명시했습니다.
- 동일 봉에서 손절과 익절이 모두 닿으면 손절 우선으로 처리합니다.
- 워크포워드와 종목 홀드아웃에 표본 수·승률·평균 R·최대 낙폭을 기록합니다.

실측 결과:

- 워크포워드: 579건, 승률 42.3%, 평균 -0.043R, 최대 낙폭 47.222R
- 종목 홀드아웃: 866건, 승률 43.1%, 평균 -0.028R, 최대 낙폭 58.550R
- 후보 임계값 0.20·0.30·0.40·0.50 모두 전체 평균 기대값이 0R 이하였습니다.
- 따라서 4축 합성 결과는 현재 상태 설명에만 사용하고, 검증된 매매 전략이나 승률로 표현하지 않습니다.
- 상세 재현 결과는 `backtest/results/signal-report.md`와 `signal-report.json`에 저장했습니다.

수정 범위:

- `강한 매수/매도`를 확률 표현이 아닌 `추세 우위/관망`으로 우선 조정합니다.
- 추세, 모멘텀, 변동성, 거래량을 각 한 축으로 정규화하여 중복 가중을 줄입니다.
- 파동 기대값 표는 검증된 일봉에서만 사용하고 다른 시간봉에서는 `미검증`으로 표시합니다.
- 백테스트 스크립트와 고정 데이터 스냅샷을 추가합니다.
- 수수료, 슬리피지, 다음 봉 체결, 손절·익절 우선순위 규칙을 명시합니다.
- 워크포워드와 심볼 홀드아웃 결과에 표본 수, 승률, 기대값, 최대낙폭을 함께 표시합니다.

검증 기준:

- 미래 봉을 사용하지 않는 순차 실행 방식이어야 합니다.
- 파라미터 선택 구간과 평가 구간을 분리해야 합니다.
- 결과를 같은 데이터와 명령으로 재현할 수 있어야 합니다.
- 표본 수가 부족한 조합은 신호를 만들지 않아야 합니다.

### 4단계 — 데이터 출처와 다중 시간봉 종합 ✅ 완료

완료일: 2026-08-17

구현 결과:

- 선택된 코인, 실제 캔들 거래소, `USDT 무기한`, 시간봉, 조회시각을 상단에 함께 표시합니다.
- 확정봉 신호에는 거래소·시장 종류·봉 마감시각을 표시합니다.
- 거래소 폴백이 발생하면 실제로 성공한 거래소를 상태에 저장하고, 펀딩비·OI·24시간 고저도 그 거래소에서만 조회합니다.
- 분할 차트 실시간 가격을 MEXC 한 곳에서 일괄 덮어쓰던 방식을 제거했습니다.
- 분할 패널은 Bybit·Binance·OKX·MEXC 중 해당 패널의 캔들 거래소와 같은 티커만 사용합니다.
- 거래소별 티커 실패 시 다른 거래소 가격으로 대체하지 않고 `데이터 없음` 또는 `지연`으로 표시합니다.
- OI는 가능한 경우 동일 거래소 가격으로 USDT 명목가치를 계산하고 원시 수량·단위를 툴팁에 보존합니다.
- Binance와 Bybit는 공개 OI 이력으로 1시간 변화율을 계산합니다.
- MEXC처럼 계약승수가 확인되지 않은 OI는 임의 환산하지 않고 계약 수와 `명목환산 불가`를 표시합니다.
- 실패한 캔들·선물 값은 이전 값이 정상값처럼 남지 않도록 즉시 지웁니다.
- 1H·4H·12H·주봉·월봉을 같은 거래소에서 독립 조회하고 각 확정봉으로 4축 방향을 계산합니다.
- 서로 반대 방향 시간봉이 공존하면 평균으로 숨기지 않고 `시간봉 방향 충돌`로 표시합니다.
- 60봉 미만 시간봉은 `표본 부족`, API 실패는 `데이터 없음`으로 표시합니다.
- 브라우저 검증에서 BTC 메인은 Bybit OI 명목가치와 1시간 변화, 5개 시간봉이 같은 출처로 표시됐습니다.
- Binance 분할 패널에서도 상단·사이드바·다중 시간봉 출처가 동일하게 유지됨을 확인했습니다.

수정 범위:

- 코인별 거래소, 현물/무기한 선물, 데이터 기준시각을 표시합니다.
- 거래소 폴백이 발생하면 차트와 선물 데이터의 거래소가 달라지지 않게 고정합니다.
- OI는 거래소별 단위를 명시하고 가능하면 USDT 명목가치와 변화율로 정규화합니다.
- 1시간·4시간·12시간·주봉·월봉을 독립 계산한 뒤 방향 합의와 충돌을 요약합니다.
- 데이터가 없거나 API가 실패하면 이전 값을 남기지 않고 `데이터 없음` 또는 `갱신 실패`로 표시합니다.

검증 기준:

- 모든 분석 카드에 거래소와 기준시각이 표시되어야 합니다.
- 실패한 데이터가 정상값처럼 남아 있지 않아야 합니다.
- 서로 다른 거래소의 가격·OI·펀딩비를 하나의 신호에 섞지 않아야 합니다.

## 3. 현재 진행 상태

- 1단계, 2단계, 3단계 구현과 검증을 완료했습니다.
- 다음 단계는 별도 승인을 받은 뒤 4단계 데이터 출처·거래소 일관성·다중 시간봉 종합을 진행합니다.
- 기존 사이트의 디자인, 코인 검색, 분할 차트, 즐겨찾기, 매크로 카드, 배포 방식은 유지했습니다.

## 4. 백업 및 복구

- 작업 폴더: `E:\AI 관련 자료\챗지피티\코인분석\coinchart`
- 수정 전 백업: `E:\AI 관련 자료\챗지피티\backups\coinchart\2026-08-17_원본_정확도개선전`
- 1단계 백업: `E:\AI 관련 자료\챗지피티\backups\coinchart\2026-08-17_1단계완료`
- 2단계 백업: `E:\AI 관련 자료\챗지피티\backups\coinchart\2026-08-17_2단계완료`
- 3단계 백업: `E:\AI 관련 자료\챗지피티\backups\coinchart\2026-08-17_3단계완료`
- 4단계 백업: `E:\AI 관련 자료\챗지피티\backups\coinchart\2026-08-17_4단계완료`
- 각 단계 완료 전 추가 백업을 생성합니다.
- 기존 백업은 덮어쓰지 않습니다.

## 5. 주의사항

- 기술적 지표의 계산 정확도를 높여도 미래 가격 예측 정확도나 수익을 보장할 수 없습니다.
- 정확도는 `표준 수식 일치`, `확정봉 재현성`, `미래정보 미사용`, `거래비용 포함 백테스트`, `아웃샘플 성능`으로 나누어 평가해야 합니다.
- 온체인, 청산맵, VPVR 원시 체결 분포처럼 현재 API로 검증할 수 없는 데이터는 추정값으로 채우지 않습니다.

## 6. 엘리엇 파동 종목 동기화 버그 수정

작성일: 2026-08-17
상태: 구현·로컬 검증 완료, 배포 대기

### 재현 결과

- BTC에서 ETH 등 다른 종목을 선택하면 상단 종목은 즉시 바뀌지만, 새 캔들이 도착하기 전까지 `waveBox`에는 BTC 파동 가격이 남습니다.
- 새 종목의 네 거래소 캔들 조회가 모두 실패하면 이전 BTC 파동 결과가 지워지지 않아 계속 정상 분석처럼 보일 수 있습니다.
- `renderAll()`은 확정봉 배열을 `doWave(rows)`에 전달하지만 `doWave()`가 `rows===candles`로 메인 여부를 판별해, 메인 확정봉을 분할 차트 분석으로 오인합니다.
- 이 오인 때문에 메인 파동 종목 라벨과 차트 파동선 갱신이 누락됩니다.

### 수정 계획

1. `doWave()`에 종목·시간봉·메인/분할 문맥을 명시적으로 전달합니다.
2. 엘리엇 결과 DOM에 분석 종목과 시간봉을 기록해 현재 선택과 일치하는지 검증할 수 있게 합니다.
3. 종목·시간봉 변경 즉시 이전 파동선과 가격을 지우고 `{선택 종목} 캔들 로딩 중`으로 교체합니다.
4. 캔들 조회 실패 시 이전 파동 결과를 제거하고 `데이터 없음`으로 표시합니다.
5. 분할 차트는 선택된 패널의 종목·시간봉만 사이드바에 쓸 수 있게 유지합니다.
6. BTC→ETH→SOL 전환, 시간봉 변경, 강제 로드 실패를 브라우저에서 검증한 뒤 GitHub Pages에 배포합니다.

### 복구 지점

- 로컬: `E:\AI 관련 자료\챗지피티\backups\coinchart\2026-08-17_엘리엇종목버그수정전`
- GitHub: `codex/backup-pre-wave-symbol-fix-2026-08-17`
- 원본 커밋: `7566a7c68e4e05aeff350bda1ea51194f52c459c`

### 구현 및 로컬 검증 결과

- `doWave()`가 종목·시간봉·메인/분할 문맥을 명시적으로 받도록 수정했습니다.
- 결과 DOM에 `data-symbol`, `data-timeframe`, `data-state`를 기록합니다.
- 종목·시간봉 변경 즉시 이전 파동 가격과 메인 차트 파동선을 제거합니다.
- 캔들 조회 실패 시 이전 분석 대신 해당 종목의 `엘리엇 분석 데이터 없음`을 표시합니다.
- BTC→ETH→SOL 연속 전환 후 각 종목 가격대와 DOM 출처가 일치했습니다.
- SOL 1시간→4시간 전환 시 로딩 상태와 완료 결과 모두 시간봉이 일치했습니다.
- 존재하지 않는 `NOTREALUSDT`를 사용한 강제 실패에서 캔들 0개, 현재 시장 `데이터 없음`, 파동 상태 `데이터 없음`을 확인했습니다.
- 정적 회귀 테스트 `tests/wave-symbol-regression.test.js`를 추가했습니다.
